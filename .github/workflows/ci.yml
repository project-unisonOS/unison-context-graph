name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  tests-matrix:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service: [
          unison-context-graph
        ]
    env:
      OTEL_TRACES_EXPORTER: none
      OTEL_METRICS_EXPORTER: none
      OTEL_LOGS_EXPORTER: none
      OTEL_EXPORTER_OTLP_ENDPOINT: ""
      PYTHONWARNINGS: default
      UNISON_REQUIRE_CONSENT: "false"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'
          cache: 'pip'
      - name: Install base deps
        run: |
          python -m pip install --upgrade pip setuptools wheel
      - name: Install project deps
        run: |
          python -m pip install "git+https://github.com/project-unisonOS/unison-common.git@f17a28de8421fabf94118375e7be98b4ca5eee97"
          python -m pip install -r requirements.txt
          python -m pip install pytest-cov pytest-rerunfailures
      - name: Enforce timezone-aware datetimes
        run: python scripts/check_no_utcnow.py
      - name: Run tests (${{ matrix.service }})
        run: |
          pytest -q --maxfail=1 --durations=15 --reruns 1

  unison-policy-tests:
    if: ${{ github.repository == 'project-unisonOS/unison-policy' }}
    runs-on: ubuntu-latest
    needs: tests-matrix
    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping || exit 1" \
          --health-interval 5s \
          --health-timeout 3s \
          --health-retries 20
    env:
      OTEL_TRACES_EXPORTER: none
      OTEL_METRICS_EXPORTER: none
      OTEL_LOGS_EXPORTER: none
      OTEL_EXPORTER_OTLP_ENDPOINT: ""
      REDIS_HOST: 127.0.0.1
      REDIS_PORT: 6379
      UNISON_CONSENT_SECRET: test-secret
      UNISON_CONSENT_AUDIENCE: unison
      UNISON_CONSENT_DEFAULT_TTL_HOURS: 1
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'
          cache: 'pip'
      - name: Install deps
        run: |
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install "git+https://github.com/project-unisonOS/unison-common.git@eef1a7353b2c795233daf0db6079b867ff2d98ba"
          python -m pip install -r unison-policy/requirements.txt
          python -m pip install pytest-cov pytest-rerunfailures
      - name: Normalize requirements to use VCS unison-common
        run: |
          sed -i 's#\.{2}/unison-common/dist/unison_common-0\.1\.0-py3-none-any\.whl#git+https://github.com/project-unisonOS/unison-common.git@eef1a7353b2c795233daf0db6079b867ff2d98ba#g' */requirements.txt || true
      - name: Enforce timezone-aware datetimes
        run: python scripts/check_no_utcnow.py
      - name: Run tests (unison-policy)
        working-directory: unison-policy
        run: pytest -q --maxfail=1 --durations=15 --reruns 1

  tests-matrix-consent:
    runs-on: ubuntu-latest
    needs: tests-matrix
    strategy:
      fail-fast: false
      matrix:
        service: [
          unison-context-graph
        ]
    env:
      OTEL_TRACES_EXPORTER: none
      OTEL_METRICS_EXPORTER: none
      OTEL_LOGS_EXPORTER: none
      OTEL_EXPORTER_OTLP_ENDPOINT: ""
      PYTHONWARNINGS: default
      UNISON_REQUIRE_CONSENT: "true"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'
          cache: 'pip'
      - name: Install base deps
        run: |
          python -m pip install --upgrade pip setuptools wheel
      - name: Normalize requirements to use VCS unison-common
        run: |
          sed -i 's#\.{2}/unison-common/dist/unison_common-0\.1\.0-py3-none-any\.whl#git+https://github.com/project-unisonOS/unison-common.git@eef1a7353b2c795233daf0db6079b867ff2d98ba#g' */requirements.txt || true
      - name: Install project deps
        run: |
          python -m pip install "git+https://github.com/project-unisonOS/unison-common.git@eef1a7353b2c795233daf0db6079b867ff2d98ba"
          python -m pip install -r requirements.txt
          python -m pip install pytest-rerunfailures
      - name: Enforce timezone-aware datetimes
        run: python scripts/check_no_utcnow.py
      - name: Run tests with consent enabled (${{ matrix.service }})
        run: |
          pytest -q --maxfail=1 --durations=15 --reruns 1
